{
  "name": "debates",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "convex dev --typecheck=disable",
    "typecheck": "convex typecheck",
    "test": "vitest run && node scripts/convex-auth-check.mjs"
  },
  "dependencies": {
    "convex": "^1.42.3"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^26.1.1",
    "@typescript/typescript6": "^6.0.2",
    "convex-test": "^0.0.54",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
